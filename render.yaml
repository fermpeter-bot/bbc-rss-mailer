services:
  - type: cron
    name: bbc-rss-mailer
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python send_digest.py
    schedule: "0 5 * * *"   # kör varje dag 07:00 svensk tid (05:00 UTC)
